---
- name: Show interface information
  hosts: "{{ cisco_asa }}"
  connection: network_cli
  #port: 22
  gather_facts: no

  tasks:
    - name: Command sh ip int br
      asa_config:
        authorize: yes
        commands:
          - show version
